[project]
name = "tack"
version = "0.1.0"
description = "tack — alias for pgc (try before you rename)"
requires-python = ">=3.11"
dependencies = [
    "pgc-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tack"]
